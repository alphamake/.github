# .github

Organization-level workflow templates and shared community health files for Alphamake.

## Responsibilities

This repository owns:

- workflow templates shown to new repositories in the organization
- organization-wide documentation for repository bootstrap

This repository does not own CI/CD implementation logic.
Reusable workflows and helper scripts live in `alphamake/automation`.
